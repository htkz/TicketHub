import React from 'react';
import Header from '../../components/UI/Header/Header';

const main = (props) => {
    return (
        <React.Fragment>
            <Header />
        </React.Fragment>
    )
};

export default main;
