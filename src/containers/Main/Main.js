import React from 'react';
import Header from '../../components/Header/Header';

const main = (props) => {
    return (
        <React.Fragment>
            <Header isUser />
        </React.Fragment>
    )
};

export default main;
