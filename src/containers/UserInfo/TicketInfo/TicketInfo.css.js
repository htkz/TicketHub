const useStyles = (theme) => ({
    root: {
        maxHeight: '80vh',
        width: '100%',
        border: '1px solid red',
        margin: theme.spacing(1),
        overflow: 'scroll',
    },
});

export default useStyles;